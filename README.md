# `themes.nvim`

Color schemes for `NeoVIM>=0.12`

## Installation

Add the following to `~/.config/nvim/init.lua`

```lua
vim.pack.add({
  "https://github.com/luiarthur/themes.nvim",
  -- Optionally, add other packages
})
vim.cmd.colorscheme("noir")
```
